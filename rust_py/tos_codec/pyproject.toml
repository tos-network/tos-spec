[build-system]
requires = ["maturin>=1.4"]
build-backend = "maturin"

[project]
name = "tos-codec"
version = "0.1.0"
requires-python = ">=3.11"
classifiers = [
  "Programming Language :: Python",
  "Programming Language :: Rust",
]

[tool.maturin]
module-name = "tos_codec"
